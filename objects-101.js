// challenge

let person = {
    firstname : 'King',
    age : 27,
    location : 'Ks'
}

console.log(`${person.firstname} is ${person.age} and lives in ${person.location}`)

person.age = person.age + 1;
console.log(`${person.firstname} is ${person.age} and lives in ${person.location}`)